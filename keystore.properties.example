# Copiá este archivo a "keystore.properties" (sin .example) y completá
# tus datos reales. keystore.properties NO debe subirse a un repo público:
# ya está agregado a .gitignore.

storeFile=../tvlive-release.jks
storePassword=TU_PASSWORD_DE_KEYSTORE
keyAlias=tvlive
keyPassword=TU_PASSWORD_DE_KEY
